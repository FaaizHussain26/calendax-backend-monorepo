export enum PatientStatusEnum {
  lead_received = "lead received",
  call_complete = "call complete",
  pre_screening_visit = "pre screening visit",
  site_visit_completed = "site visit completed",
  no_show = "no show",
  no_response = "no response",
}

export enum BestTimeToCallEnum {
  morning = "morning",
  afternoon = "afternoon",
  evening = "evening",
}