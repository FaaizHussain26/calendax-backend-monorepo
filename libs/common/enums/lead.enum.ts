export enum LeadSource {
  MANUAL = 'manual',
  FACEBOOK = 'facebook',
}

export enum LeadStatus {
  PENDING = 'pending',
  CALLING = 'calling',
  SCREENED = 'screened',
  CONVERTED = 'converted',
  REJECTED = 'rejected',
}