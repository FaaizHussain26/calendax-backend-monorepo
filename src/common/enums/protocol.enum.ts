
export enum ProtocolStatus {
  ORIGINAL = 'original',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  REMOVED = 'removed',
  UPDATED = 'updated',
}
export enum ProtocolDocumentStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
